# PageMate Phase 2 체크리스트

> 기획서(PRD · 요구사항정의서 · TRD) ↔ 현재 구현 상태를 대조해 **Phase 2에서 다룰 항목**을 정리한 문서입니다.
> Phase 1(Must 기능 동작)은 대체로 완료된 상태이며, 이 문서는 **완성도(Should) 및 문서 정합성** 확보에 초점을 둡니다.
>
> 작성일: 2026-07-27 · 기준 브랜치: `feat/wireframe-polish`

---

## 0. 한눈에 보기

| 구분 | 항목 수 | 핵심 내용 |
|------|--------|-----------|
| 🔴 A. Must 사양 갭 | 4 | 위치 정책(✅확정) · 컨디션(✅미진행) · 독서기록(✅제외 확정) · 취향 유사 독자(✅Phase 2 이월) — **A군 방향 결정 완료** |
| 🟡 B. Should 미구현 | 6 | FCM 푸시 · 알림설정 · 이미지 업로드 · 채팅 이미지 · 계정 탈퇴 · 온보딩 |
| 🟢 C. 문서 정합성 | 5 | 구현이 기획서를 앞서간 부분 + 대여 모델 용어 통일 → 문서 현행화 필요 |
| ⚙️ D. 비기능/기술 점검 | 5 | 테스트 · 성능 · CI/CD · Swagger 등 |

---

## 🔴 A. Must 사양인데 빠졌거나 방향이 바뀐 것 (최우선 결정 필요)

> Phase 2 진입 전에 **"기획서를 구현에 맞춰 개정할지 vs 구현을 기획에 맞춰 되돌릴지"** 방향부터 정해야 하는 항목들입니다.

- [x] **A-1. 위치 기반 접근 정책** (F-BOOK-06, 요구사항 핵심가치 "반경 2km") — ✅ **결정 완료**
  - 현재: 좌표(lat/lng) 정렬 미구현. `sort` 파라미터는 받기만 하고 무시, 항상 `createdAt DESC`. books의 lat/lng 컬럼은 V10에서 제거되고 `neighborhood` 문자열 LIKE 검색으로 대체. → 사실상 이미 "전체 노출 + 동네 선택 필터" 구조.
  - **결정**: 콜드 스타트(초기 저밀도) 대응을 위해 **MVP는 전국/전체 노출**로 확정. 반경 하드 필터는 걸지 않는다.
    - 동네(`neighborhood`)는 **하드 필터가 아니라 선택 필터/정보 표시용**으로만 사용 (카드에 "○○동" 뱃지 수준).
    - **위치 데이터(유저 `location`/`lat`/`lng`)는 계속 수집·저장** — 지역화 전환 시 소유자 위치 기준으로 거리 계산이 가능해야 하므로 지금부터 쌓아둔다.
    - 도서에 좌표 컬럼을 되살리지 않는다 (소유자 위치로 대체 가능).
  - **지역화 전환 트리거(밀도 기준, 잠정)**: 아래 조건을 만족하는 지역부터 반경 필터를 *옵션*으로 노출 → 안정화되면 기본값으로 승격.
    - 단일 행정동 기준 **활성 매물 30건 이상** *또는* **최근 30일 활성 가입자 50명 이상**
    - 전국 누적 가입자 **1,000명**(PRD KPI) 도달 시 지역화 A/B 테스트 시작
    - ※ 숫자는 초기 운영 데이터 확인 후 재보정.
- [x] **A-2. 도서 컨디션(상태)** (F-BOOK-02 / F-BOOK-07) — ✅ **미진행 확정**
  - 현재: `book_condition` 컬럼이 V8에서 제거됨. 등록·수정·검색 모두 컨디션 없음.
  - **결정**: 컨디션 등록/필터 **도입 안 함**. 이유 ① 등급 기준이 사람마다 달라 주관적 → 분쟁·혼란 유발. ② PageMate는 소유권을 넘기는 중고 "교환"이 아니라 **빌려주고 돌려받는 대여 모델**이라 매물 상태를 관리할 필요가 약함.
  - 훼손 리스크는 별도 컨디션 시스템이 아니라 **반납 확인 + 상호평가(Review)** 로 커버.
- [x] **A-3. 독서 기록** (F-READ-01~03, PRD 4대 핵심기능) — ✅ **스코프 제외 확정**
  - 현재: 도메인·테이블·화면 전부 없음. 교환 후 **상호평가(Review)** 로 대체됨. FE엔 "독서 카드 이미지 공유"만 존재(저장 X, 별개 기능).
  - **결정(2026-08)**: 별도 독서 기록 도메인 **미도입 확정**. 근거 ① 교환용으로 **등록한 도서 목록 자체가 "읽은 책" 기록**이라 이걸로 취향 파악이 됨 → 따로 기록할 필요 없음. ② "읽은 책 + 별점·감상" 니즈는 **상호평가(Review)** 로 충족.
  - 문서 현행화 완료: `requirements.md`(§3.5, US-09/10/12, S-07, 도메인표, 마일스톤), `PageMate_PRD_v3.5.md`(확장기능표·Phase전략)에서 F-READ 제외 반영.
- [x] **A-4. 홈 "취향 유사 독자" 노출** (S-01, 요구사항 핵심가치 "취향 기반 연결") — ✅ **Phase 2 이월 확정**
  - 현재: 홈에 해당 섹션·API 없음.
  - **결정(2026-08)**: 유저↔유저 추천은 **콜드 스타트 문제**(초기 저밀도 → 매칭 품질 낮음)로 MVP 제외, **A-1과 동일한 밀도 트리거**에 묶어 Phase 2 이월. 단 취향 시그널(장르·태그·등록 도서)은 계속 수집.
  - ※ 향후 대안: 유저 매칭 대신 **콘텐츠 기반 "취향 맞는 책" 추천**(등록 도서 ↔ 내 장르/태그)은 콜드 스타트에 강해 우선 검토 가능.

---

## 🟡 B. Should 미구현 — Phase 2 실제 작업 대상

> 기획서상 Phase 2(완성도) 범위. 아래는 **실제로 구현해야 할 작업 리스트**입니다.

- [x] **B-1. 푸시 알림** (F-NOTI-03, US-11) — ✅ **구현 완료 (Expo Push 방식)**
  - **결정**: firebase-admin 대신 **Expo Push**(exp.host) 채택 — Expo 관리형 앱에 최적, APNs 키를 EAS가 자동 관리(수동 Firebase 불필요). firebase-admin 의존성 제거.
  - BE: `POST /v1/users/me/push-token`(토큰 등록) + `ExpoPushClient`(@Async 발송) + `NotificationService.send()`에 배선 → 교환 전 이벤트 + **채팅 신규 메시지**(ChatService에 CHAT_MESSAGE 알림 신설)까지 푸시.
  - FE: `expo-notifications`/`expo-device` 설치, `src/lib/push.ts`(권한·토큰·등록·딥링크), `(tabs)/_layout`에서 로그인 후 등록 + 알림 탭 시 딥링크(채팅→방, 교환→swap).
  - ⚠️ 반영 조건: FE 네이티브 모듈 추가 → **새 EAS 빌드 필요** (실기기에서만 동작).
- [x] **B-2. 알림 on/off 설정** (F-NOTI-04, US-13) — ✅ **구현 완료 (마스터 토글)**
  - BE: `users.push_enabled` 컬럼(V17, 기본 TRUE) + `PATCH /v1/users/me/notification-setting` + `ProfileResponse.pushEnabled`. `NotificationService.send()`가 **push_enabled=false면 푸시 스킵**(인앱 알림함엔 계속 저장).
  - FE: 마이 화면 "알림 설정" 행에 `Switch` 연결(낙관적 업데이트+실패 롤백). 끄면 "꺼짐" 표시.
  - ※ 스코프: 타입별(교환/채팅 분리) 토글이 아닌 **전체 푸시 on/off** 마스터 토글 1개. 세분화는 후속 개선.
> **이미지 저장소(2026-08)**: AWS S3 → **Cloudinary**로 교체. `ImageStorage` 인터페이스 + `CloudinaryStorage`(S3Service 제거). `CLOUDINARY_URL` 환경변수로 활성화(Railway 설정됨), 미설정 시 no-op. 프로필 이미지 업로드 **실동 검증 완료**.

- [x] **B-3. 도서 이미지 직접 업로드** (F-BOOK-03) — ❌ **스코프 제외 확정**
  - 결정(2026-08): 카카오 썸네일 URL로 충분 → 직접 업로드 미도입. (저장소는 준비돼 있어 필요 시 나중에 연결 가능)
- [x] **B-4. 채팅 이미지 메시지** (F-CHAT-05) — ✅ **구현 완료**
  - `MessageType.IMAGE` + FE 이미지 버블 렌더링 + 시드 이미지 메시지(V19).
  - 앱에서 이미지 선택→Cloudinary 업로드→전송(WebSocket 브로드캐스트) 경로 완성. `imageStorage.upload(file, "chat")` 연결.
- [x] **B-5. 계정 탈퇴** (US-14) — ✅ **구현 완료** (심사 지침 5.1.1(v))
  - BE: `DELETE /v1/users/me` + `ProfileService.deleteAccount()` — 개인정보 제거 + 진행 중 대여 취소 + soft delete(`deleted_at`, V17). 탈퇴 사용자는 도서 목록·프로필 조회에서 제외, refresh 토큰 무효화(`isDeleted` 체크).
  - FE: 마이 화면 "회원 탈퇴" 2단계 확인 후 호출.
- [x] **B-6. 온보딩 프로필 설정 단일화** (F-AUTH-06) — ✅ **구현 완료**
  - signup.tsx를 **3스텝 단일 온보딩**(닉네임 → 동네 → 취향 장르, 진행 인디케이터)으로 재작성. 마지막에 `updateMyProfile({nickname, location, tags})` 한 번으로 저장.
  - 기존 분산 흐름 제거: `storage.setGenrePending()` 미사용 → 홈 장르 바텀시트는 신규 유저에게 더는 뜨지 않음(온보딩에서 수집). ※ 홈의 해당 시트 코드는 무해하게 잔존(후속 정리 가능).

---

## 🟢 C. 문서 정합성 — 구현이 기획서를 앞서간 부분 (문서 현행화)

> 이미 구현됐으나 기획 문서에 반영되지 않은 것들. 방치하면 기획-구현 괴리가 계속 커집니다.

- [ ] **C-1. 상호평가(Review) 도메인** — 별점 1~5 + 코멘트, 사용자 받은 리뷰 목록까지 구현됨. 기획서엔 Post-MVP "별점 & 리뷰"로 분류돼 있어 미반영. → **정식 기능으로 승격 반영**.
- [ ] **C-2. 교환 플로우 확장** — 기획은 요청/수락/거절/완료 4단계지만 실제는 `PENDING → ACCEPTED → PLEDGED → SCHEDULED → FIRST_EXCHANGED → SECOND_EXCHANGED` 다단계 + 약속문 동의 + 일정 확정 + 반납기한 스케줄러. → **PRD/api-spec에 상태머신 반영**.
- [ ] **C-3. 위치(Location) 도메인** — 동네 검색·역지오코딩 API 신규 추가. 기획서에 없음. → **문서 추가**.
- [ ] **C-4. 프로필 이미지 업로드** — 실제 구현됨(S3). ※ 스코프 제외로 관리하던 항목이므로 문서·내부 기록과 실제 구현 상태 일치시키기.
- [ ] **C-5. "교환" vs "대여-반납" 모델 불일치** — *우선순위 High (제품 정체성)*
  - 실제 제품은 소유권을 넘기는 중고 "교환"이 아니라 **빌려주고 돌려받는 대여(반납) 모델**. 코드도 이에 부합(`due_date`, `ExchangeDueDateScheduler`, 1·2차 반납 확인 `FIRST_EXCHANGED`/`SECOND_EXCHANGED`).
  - 그러나 PRD·요구사항·TRD는 전부 "교환/맞바꿈"으로 서술 → 정체성과 문서가 어긋남.
  - 할 일: 기획서 3종의 용어·플로우를 대여 모델로 통일. UI 카피("교환하기" 등)도 함께 점검.

---

## ⚙️ D. 비기능 / 기술 점검 (TRD 대비 — 확인 필요)

> Phase 2 QA·안정화 단계에서 함께 점검할 항목입니다.

- [ ] **D-1. 테스트 커버리지** — TRD 목표: JUnit 70% / Jest 컴포넌트 / Detox E2E. 실제 커버리지 확인 필요.
- [x] **D-2. 성능 — 위치 쿼리 SPATIAL INDEX** — A-1 결정(전체 노출)에 따라 **지역화 전환 시점까지 보류**. 비기능 요구사항에서 "반경 2km 쿼리 최적화" 항목은 지역화 단계로 이월.
- [ ] **D-3. CI/CD** — GitHub Actions 빌드/테스트 파이프라인 존재 여부 확인.
- [ ] **D-4. API 문서(Swagger)** — SpringDoc 자동 문서화 상태 확인.
- [ ] **D-5. 검색 정렬 버튼** — FE에 UI만 있고 비동작. A-1과 연결해 처리.

---

## 권장 진행 순서

1. **A군 방향 결정** — A-1 위치(✅ 전체 노출로 확정) / A-2 컨디션 / A-3 독서기록 / A-4 취향 독자. 나머지도 스펙 확정 없이는 다른 작업이 흔들림.
2. **C군 문서 현행화** — 결정 사항을 PRD·요구사항·TRD·api-spec에 즉시 반영.
3. **B군 구현** — B-1(푸시) · B-2(알림설정) · B-5(탈퇴) 우선(심사·사용자 기대치), 이후 B-3 · B-4 · B-6.
4. **D군 점검** — 안정화·QA 단계에서 병행.
